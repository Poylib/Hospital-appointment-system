import styled from 'styled-components';

const UserInfo = () => {
  return (
    <StyledUserInfo>
      <div>dsad</div>
    </StyledUserInfo>
  );
};

export default UserInfo;

const StyledUserInfo = styled.div``;
